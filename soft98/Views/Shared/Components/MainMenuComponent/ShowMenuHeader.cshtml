@model IEnumerable<soft98.DataAccessLayer.Entities.Category>

@foreach (var item in Model)
{
    <div class="col-lg-2 " style="direction: rtl;display: inline-block;">
        <div class="col-lg-12" style="text-align: center; margin-top: 18px; width: 175px;">
            <a href="" class="Home headerIconLogin">@item.Name</a>
        </div>

        <div class="col-lg-12" style="margin-right: 70px; position: relative; top: -4px;">
            <i class="fas fa-sort-down arrow-headers text-center"></i>
        </div>
    </div>
}
